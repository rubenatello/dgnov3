// Legacy file preserved as a compatibility re-export.
// The header was modularized to `src/components/header/Header.tsx` — export that here
// so any old imports referencing `src/components/Header` continue to work.
export { default } from './header/Header';
